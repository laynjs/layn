## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] `pnpm check` passes (lint, format, LOC)
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] `pnpm build` passes
- [ ] Added a changeset (`pnpm changeset`) if a published package changed
- [ ] Updated documentation if the public API changed
