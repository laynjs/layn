---
'@laynjs/qwik': minor
---

Version bumped in lockstep with `@laynjs/core`. Qwik gained the `direction` option; sticky headers and
per-item drag remain unavailable there, because the adapter renders without per-item element refs to
stay resumable.
